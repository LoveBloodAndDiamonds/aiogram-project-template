# Aiogram Project Template

Шаблон Telegram-бота на **Aiogram 3** с готовой инфраструктурой: асинхронная работа с БД, типизированные middleware, рендер шаблонов ответов и настроенный пайплайн разработки. Репозиторий подходит для быстрого старта pet-проектов и продакшен-ботов.

## 🛠 Включенные инструменты

- **Aiogram 3** — современный фреймворк для Telegram-ботов с `Router`/`Middleware` архитектурой
- **SQLAlchemy 2 + asyncpg** — асинхронный доступ к PostgreSQL через абстракцию `Database`
- **Alembic** — управление миграциями и готовый `Makefile` для их создания/применения
- **Loguru** — настраиваемый логгер с ротацией файлов и разноуровневым выводом
- **uv** — быстрый менеджер зависимостей и универсальная CLI для запуска команд
- **Ruff** и **BasedPyright** — форматирование, линтинг и строгая проверка типов
- **Docker + docker-compose** — локальный стек (бот + PostgreSQL) с томами для кода и логов
- **Pre-commit** — автоматическая проверка качества перед коммитами

## 📝 Как использовать

1. Склонируйте репозиторий
```bash
git clone https://github.com/LoveBloodAndDiamonds/aiogram-project-template.git new-bot
cd new-bot
```

2. Установите зависимости и активируйте окружение
```bash
pip3 install uv                      # устанавливаем uv
uv sync                              # подтягиваем зависимости из pyproject.toml
uv venv && source .venv/bin/activate # создаём и активируем виртуальное окружение
```

3. Настройте окружение
```bash
cp .env.dist .env        # переносим шаблон
# заполните BOT_TOKEN, ADMIN_ID и доступы к PostgreSQL
```

4. Поднимите PostgreSQL и примените миграции
```bash
docker compose up -d postgres        # поднимаем базу
uv run alembic upgrade head          # накатываем миграции
```

5. Запустите бота
```bash
uv run python -m bot                 # локальный запуск
# или docker compose up --build bot  # контейнерный запуск бота
```

## 🔧 Установка и настройка инструментов

### uv
- уже используется во всех командах; при необходимости глобально обновляется через `pipx install uv`.
- любые команды проекта запускаются через `uv run <cmd>`.

### Ruff
```bash
uv tool install ruff         # установка
ruff check .                 # линтинг
ruff format .                # автоформатирование
```
Конфигурация в `ruff.toml` — сортировка импортов, форматирование и общие правила стиля.

### BasedPyright
```bash
uv tool install basedpyright
basedpyright
```
`pyrightconfig.json` включает строгий режим, пути к коду и игнор виртуальных окружений.

### Alembic
Миграции создаются и применяются через `Makefile`.
```bash
make create-migrations  # автогенерация (попросит название миграции)
make migrate            # применение (uv run alembic upgrade head)
```
Шаблоны миграций и окружение уже настроены под `bot/database`.

### Pre-commit
```bash
uv tool install pre-commit
pre-commit install
```
Хуки автоматически запускают Ruff и другие проверки перед коммитами.

## 🐳 Docker

Стек описан в `docker-compose.yaml`:
- `postgres` — база с healthcheck и сохранением данных в `postgres_data`.
- `bot` — контейнер с горячей подгрузкой каталога `bot/` и логами в `logs/`.

Запуск:
```bash
docker compose up --build
```
При необходимости можно стартовать только базу (`docker compose up -d postgres`) и работать локально через `uv`.

## 🔍 Структура проекта

```
.
├── bot/                     # исходники бота
│   ├── __main__.py          # точка входа и настройка Dispatcher
│   ├── config/              # загрузка .env и фабрика логгеров
│   ├── handlers/            # команды /start, /help, обработка нераспознанных событий
│   ├── middlewares/         # логирование, обработка ошибок, проброс Database
│   ├── filters/             # Admin/ChatType фильтры
│   ├── database/            # модели, репозитории и Alembic
│   ├── schemas/             # TypedDict для передачи данных между слоями
│   └── templates/           # текстовые шаблоны ответов
├── logs/                    # директория для логов
├── Makefile                 # команды миграций
├── docker-compose.yaml      # бот + PostgreSQL
├── pyproject.toml           # зависимости (aiogram, SQLAlchemy, loguru и др.)
├── pyrightconfig.json       # конфигурация BasedPyright
├── ruff.toml                # настройки Ruff
└── .env.dist                # шаблон переменных окружения
```

## 📋 Рекомендации по использованию

1. Создавайте новые пакеты внутри `bot/handlers` по функциональным областям и подключайте их в `register_handlers`.
2. Используйте `Database.session_context()` или `DatabaseMiddleware`, чтобы избегать ручного управления сессиями.
3. Логи храните в `logs/<name>/<level>.log`, уровень вывода задаётся в `Configuration.LOGGING_LEVEL`.
4. Регулярно запускайте `ruff check .` и `basedpyright`, чтобы ловить проблемы до деплоя.

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие шаблона! Если у вас есть предложения по улучшению:

1. Сделайте форк репозитория
2. Создайте ветку для ваших изменений
3. Отправьте pull request
